{
  "name": "spec-axis",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -C apps/studio dev",
    "build": "pnpm -C apps/studio build",
    "start": "pnpm -C apps/studio start",
    "lint": "pnpm -C apps/studio lint",
    "dev:studio": "pnpm -C apps/studio dev",
    "build:studio": "pnpm -C apps/studio build",
    "start:studio": "pnpm -C apps/studio start",
    "lint:studio": "pnpm -C apps/studio lint",
    "codebase:cleanup": "node apps/studio/src/scripts/codebase-cleanup.mjs"
  }
}
